# Copy to .env.local and paste a real key. Free tier at console.groq.com/keys
# Without this the voice layer still runs — it just drops to the keyword tier
# and can only answer the fixed commands.
VITE_GROQ_KEY=gsk_your_key_here
